class Book:
    welcome = "/"
    create_book = "/book"
    read_book = "/books_all"
    update_book = "/books/{title}"
    delete_book = "/books/{title}"
    send_email = "/send_email"
    pipe_agg = "/total_price"
